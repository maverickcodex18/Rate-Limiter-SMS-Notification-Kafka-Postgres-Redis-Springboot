# ============================================================================
# RateLimitedSMSGateway — Environment Configuration Template
# ============================================================================
#
# SETUP:
#   1. Copy this file:  cp .env.template .env
#   2. Fill in the values below (especially BASE_DIR)
#   3. .env is git-ignored — your credentials stay local
#
# CRITICAL: BASE_DIR must not be empty. Docker Compose will fail without it.
# ============================================================================

# ----------------------------------------------------------------------------
# BASE DIRECTORY (REQUIRED)
# ----------------------------------------------------------------------------
# Absolute path where Docker volume data is stored on your host machine.
# Docker Compose creates subdirectories: build/, zookeeper/, kafka/, postgres/, redis/
#
# Use forward slashes (/) on all platforms for compatibility.
#
# Windows:  BASE_DIR=C:/Projects/RateLimitedSMSGateway/docker_data
# Linux:    BASE_DIR=/home/youruser/RateLimitedSMSGateway/docker_data
# macOS:    BASE_DIR=/Users/youruser/RateLimitedSMSGateway/docker_data
#
BASE_DIR=

# ----------------------------------------------------------------------------
# POSTGRESQL (REQUIRED)
# ----------------------------------------------------------------------------
# These credentials are used by both the Postgres container and the
# Spring Boot application. They must match.
#
POSTGRES_USER=myuser
POSTGRES_PASSWORD=secret
POSTGRES_DB=RateLimitedSMSGateway

# ----------------------------------------------------------------------------
# REDIS (OPTIONAL)
# ----------------------------------------------------------------------------
REDIS_PASSWORD=redisPassword
